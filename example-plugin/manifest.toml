name = "weather"
version = "0.1.0"
description = "Example weather tool plugin for OpsClaw"
author = "OpsClaw"
wasm_path = "target/wasm32-wasip1/release/opsclaw_weather_plugin.wasm"

capabilities = ["tool"]
permissions = ["http_client"]
