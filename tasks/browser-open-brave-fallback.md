# Browser-open Brave fallback should be removed

Three `// TODO(compat)` markers in `tools/browser_open.rs:136,187,208` note that the Brave Browser fallback should be removed once default-browser launch is stable. Verify stability and clean up.
