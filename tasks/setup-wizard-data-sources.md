# `data_sources` config has no interactive setup path

Prometheus, Seq, Jaeger, Elasticsearch URLs must be hand-edited in `config.toml`. The setup wizard should optionally collect data source endpoints (at least Prometheus, since it is the most common).
