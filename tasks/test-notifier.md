# `ops/notifier.rs` has no tests

The `AlertNotifier` trait and its default `notify_with_buttons` fallback are untested. Add unit tests for the text-fallback path and for the `format_alert_message` helper.
